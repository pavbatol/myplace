server:
  port: 8080

api:
  prefix: /api/v1

spring:
  application:
    name: mp-gateway
  banner:
    location: banner/banner-dev.txt
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false

logging:
  level:
    ru.pavbatol: DEBUG
    org.springframework.web: DEBUG